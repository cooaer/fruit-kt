rootProject.name = "fruit-kt"
include(":fruit-ksp")
include(":converter-retrofit")

pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
    }
}
